# Tests for tfConstrainedGauss

These are the tests for the `tfConstrainedGauss` package. They are run using `pytest`:
```
pytest .
```
Or run individual tests:
```
pytest fname.py -s -k "test_name"
```